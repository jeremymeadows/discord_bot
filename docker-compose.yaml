services:
  bot:
    container_name: old_tom
    image: old-tom:latest
    build: .
    restart: always
    volumes:
      - ./.env:/app/.env
      - ./timezones.db:/app/timezones.db
      - ./dynamic-channels.db:/app/dynamic-channels.db
      - ./events.db:/app/events.db
